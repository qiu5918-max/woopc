---
import { CURRENT_SUBDOMAIN, API_BASE } from '../lib/tenant-config';

// 从 API 获取租户配置
async function getSiteConfig() {
  try {
    const res = await fetch(`${API_BASE}/api/v1/public/${CURRENT_SUBDOMAIN}`, {
      headers: { 'Content-Type': 'application/json' }
    });
    if (!res.ok) return null;
    const data = await res.json();
    return data.data;
  } catch (error) {
    console.error('Failed to fetch site config:', error);
    return null;
  }
}

// 从 API 获取荣誉数据
async function getHonors() {
  try {
    const res = await fetch(`${API_BASE}/api/v1/public/${CURRENT_SUBDOMAIN}/honors`, {
      headers: { 'Content-Type': 'application/json' }
    });
    if (!res.ok) return [];
    const data = await res.json();
    return data.data || [];
  } catch (error) {
    console.error('Failed to fetch honors:', error);
    return [];
  }
}

const siteConfig = await getSiteConfig();
const apiHonors = await getHonors();

// 默认配置（邱小亮）
const defaultHeroConfig = {
  name: '邱小亮',
  title: '企业AI智能体',
  subtitle: '实施部署专家',
  tagline: '心脑血管健康数字化管理实践者',
  description: '用AI落地能力+数字化思维，让企业AI落地有方法，心脑血管健康管理有数据支撑',
  badges: ['17年实战经验', '系统架构师', '🏝️ 海南岛AI健康大使'],
  stats: [
    { value: '17+', label: '年实战经验' },
    { value: '50+', label: '企业服务' },
    { value: '100%', label: '客户满意' }
  ],
  buttons: [
    { text: '企业AI咨询', style: 'accent', icon: 'lightning' },
    { text: '健康管理咨询', style: 'secondary', icon: 'heart' }
  ],
  centerText: 'AI',
  floatingElements: ['数据安全', '健康管理', '智能体部署', '数据分析']
};

// 恋恋配置
const lianlianHeroConfig = {
  name: '恋恋',
  title: '女性全生命周期抗衰',
  subtitle: '健康管理导师',
  tagline: '以肠为根 · 以轴为核 · 守护卵巢 · 规划一生健康',
  description: '专注女性卵巢抗衰（AMH提升与维护）、肠道微生态管理、家庭院前健康与长寿规划',
  badges: ['24年主厨经验', '6年健康管理', '🏝️ 海南岛AI健康大使'],
  stats: [
    { value: '100+', label: '服务家庭' },
    { value: '24年', label: '健康美食' },
    { value: '98%', label: '客户满意' }
  ],
  buttons: [
    { text: 'AMH管理咨询', style: 'accent', icon: 'heart' },
    { text: '肠道健康咨询', style: 'secondary', icon: 'activity' }
  ],
  centerText: '❤️',
  floatingElements: ['卵巢抗衰', '肠道健康', 'AMH管理', '家庭健康']
};

// 根据租户选择配置
const heroConfig = CURRENT_SUBDOMAIN === 'lianlian' ? lianlianHeroConfig : defaultHeroConfig;

// 如果有 API 荣誉数据，更新 badges
if (apiHonors.length > 0) {
  heroConfig.badges = apiHonors.slice(0, 3).map((h: any) => {
    const title = h.title.length > 10 ? h.title.substring(0, 10) + '...' : h.title;
    return h.type === 'AWARD' ? `🏆 ${title}` : `✓ ${title}`;
  });
}
---

<section class="min-h-[90vh] flex items-center relative overflow-hidden tech-grid">
  <!-- 科技感背景装饰 -->
  <div class="absolute inset-0 overflow-hidden">
    <!-- 渐变光晕 -->
    <div class="absolute top-1/4 -left-1/4 w-[600px] h-[600px] bg-accent/20 rounded-full blur-3xl animate-pulse-slow"></div>
    <div class="absolute bottom-1/4 -right-1/4 w-[500px] h-[500px] bg-secondary/20 rounded-full blur-3xl animate-pulse-slow" style="animation-delay: 2s;"></div>

    <!-- 浮动粒子 -->
    <div class="absolute top-20 left-10 w-2 h-2 bg-accent rounded-full animate-float"></div>
    <div class="absolute top-40 right-20 w-3 h-3 bg-primary rounded-full animate-float" style="animation-delay: 1s;"></div>
    <div class="absolute bottom-40 left-1/4 w-2 h-2 bg-secondary rounded-full animate-float" style="animation-delay: 2s;"></div>
    <div class="absolute top-1/3 right-1/3 w-4 h-4 bg-accent/50 rounded-full animate-float" style="animation-delay: 3s;"></div>

    <!-- 连接线 -->
    <svg class="absolute inset-0 w-full h-full opacity-10 dark:opacity-20" xmlns="http://www.w3.org/2000/svg">
      <defs>
        <linearGradient id="line-gradient" x1="0%" y1="0%" x2="100%" y2="100%">
          <stop offset="0%" style="stop-color:#6366F1;stop-opacity:0" />
          <stop offset="50%" style="stop-color:#6366F1;stop-opacity:1" />
          <stop offset="100%" style="stop-color:#6366F1;stop-opacity:0" />
        </linearGradient>
      </defs>
      <line x1="0" y1="200" x2="100%" y2="400" stroke="url(#line-gradient)" stroke-width="1"/>
      <line x1="100%" y1="100" x2="0" y2="300" stroke="url(#line-gradient)" stroke-width="1"/>
    </svg>
  </div>

  <div class="container relative z-10 py-16">
    <div class="grid lg:grid-cols-2 gap-12 items-center">
      <!-- 左侧：文字内容 -->
      <div class="text-center lg:text-left">
        <!-- 数据徽章 -->
        <div class="flex flex-wrap gap-2 justify-center lg:justify-start mb-6">
          {heroConfig.badges.map((badge: string) => (
            <span class="data-badge">{badge}</span>
          ))}
        </div>

        <!-- 核心头衔 -->
        <h1 class="text-3xl md:text-4xl lg:text-5xl font-bold mb-6 leading-tight">
          <span class="gradient-text">{heroConfig.title}</span>
          <br/>
          <span class="text-text-primary">{heroConfig.subtitle}</span>
        </h1>

        <p class="text-xl md:text-2xl font-medium mb-4 text-secondary">
          {heroConfig.tagline}
        </p>

        <!-- 核心价值 -->
        <p class="text-text-secondary text-base md:text-lg mb-8 max-w-xl mx-auto lg:mx-0">
          {heroConfig.description}
        </p>

        <!-- 统计数据 -->
        <div class="grid grid-cols-3 gap-4 mb-8 max-w-md mx-auto lg:mx-0">
          {heroConfig.stats.map((stat: any) => (
            <div class="text-center lg:text-left">
              <div class="stat-number">{stat.value}</div>
              <div class="stat-label">{stat.label}</div>
            </div>
          ))}
        </div>

        <!-- 双转化按钮 -->
        <div class="flex flex-col sm:flex-row gap-4 justify-center lg:justify-start">
          {heroConfig.buttons.map((btn: any, i: number) => (
            <a href="#contact" class={`btn ${btn.style === 'accent' ? 'btn-accent' : 'btn-secondary'} px-8 py-4 text-base font-semibold`}>
              {btn.icon === 'lightning' && (
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
                </svg>
              )}
              {btn.icon === 'heart' && (
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
                </svg>
              )}
              {btn.icon === 'activity' && (
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
                </svg>
              )}
              {btn.text}
            </a>
          ))}
        </div>
      </div>

      <!-- 右侧：可视化图形 -->
      <div class="hidden lg:block relative">
        <div class="relative w-full aspect-square max-w-lg mx-auto">
          <!-- 中心圆形 -->
          <div class="absolute inset-0 flex items-center justify-center">
            <div class="w-64 h-64 rounded-full bg-gradient-to-br from-accent/20 to-purple-600/20 animate-pulse-slow flex items-center justify-center">
              <div class="w-48 h-48 rounded-full bg-gradient-to-br from-accent/30 to-purple-600/30 flex items-center justify-center">
                <div class="w-32 h-32 rounded-full bg-gradient-to-br from-accent to-purple-600 flex items-center justify-center shadow-2xl shadow-accent/30">
                  <span class="text-4xl font-bold text-white">{heroConfig.centerText}</span>
                </div>
              </div>
            </div>
          </div>

          <!-- 环绕元素 -->
          <div class="absolute top-4 left-1/2 -translate-x-1/2 card p-3 animate-float">
            <div class="flex items-center gap-2">
              <div class="icon-box-secondary w-8 h-8">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />
                </svg>
              </div>
              <span class="text-sm font-medium text-text-primary">{heroConfig.floatingElements[0]}</span>
            </div>
          </div>

          <div class="absolute bottom-4 left-1/2 -translate-x-1/2 card p-3 animate-float" style="animation-delay: 1s;">
            <div class="flex items-center gap-2">
              <div class="icon-box-primary w-8 h-8">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
                </svg>
              </div>
              <span class="text-sm font-medium text-text-primary">{heroConfig.floatingElements[1]}</span>
            </div>
          </div>

          <div class="absolute left-4 top-1/2 -translate-y-1/2 card p-3 animate-float" style="animation-delay: 2s;">
            <div class="flex items-center gap-2">
              <div class="icon-box w-8 h-8">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.75 17L9 20l-1 1h8l-1-1-.75-3M3 13h18M5 17h14a2 2 0 002-2V5a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                </svg>
              </div>
              <span class="text-sm font-medium text-text-primary">{heroConfig.floatingElements[2]}</span>
            </div>
          </div>

          <div class="absolute right-4 top-1/2 -translate-y-1/2 card p-3 animate-float" style="animation-delay: 3s;">
            <div class="flex items-center gap-2">
              <div class="icon-box-secondary w-8 h-8">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
                </svg>
              </div>
              <span class="text-sm font-medium text-text-primary">{heroConfig.floatingElements[3]}</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

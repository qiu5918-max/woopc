---
import { CURRENT_SUBDOMAIN, API_BASE, type CoreCapability, type Achievement } from '../lib/tenant-config';

// 从 API 获取租户配置
async function getTenantConfig() {
  try {
    const res = await fetch(`${API_BASE}/api/v1/public/${CURRENT_SUBDOMAIN}`, {
      headers: { 'Content-Type': 'application/json' }
    });
    if (!res.ok) return null;
    const data = await res.json();
    return data.data;
  } catch (error) {
    console.error('Failed to fetch tenant config:', error);
    return null;
  }
}

// 从 API 获取荣誉数据
async function getHonors() {
  try {
    const res = await fetch(`${API_BASE}/api/v1/public/${CURRENT_SUBDOMAIN}/honors`, {
      headers: { 'Content-Type': 'application/json' }
    });
    if (!res.ok) return [];
    const data = await res.json();
    return data.data || [];
  } catch (error) {
    console.error('Failed to fetch honors:', error);
    return [];
  }
}

const tenantConfig = await getTenantConfig();
const apiHonors = await getHonors();

// 荣誉类型 emoji 映射
const honorTypeEmoji: Record<string, string> = {
  'CERTIFICATE': '🏆',
  'AWARD': '🎖️',
  'PARTNERSHIP': '🤝',
  'PATENT': '📜',
  'OTHER': '⭐'
};

// 默认核心能力（邱小亮）
const defaultCoreCapabilities: CoreCapability[] = [
  {
    id: 'ai',
    title: '企业AI智能体',
    subtitle: '实施部署专家',
    tagline: '实战派，落地为核心',
    description: '从需求拆解 → 方案定制 → 技术落地 → 效果复盘，全流程闭环',
    highlights: [
      { icon: '💼', label: '17年', text: 'AI产品/政企项目实战' },
      { icon: '🏛️', label: '10年', text: '政企合作经验' },
      { icon: '🎓', label: '持证', text: '系统架构设计师+数据资产管理师' },
      { icon: '👥', label: '90%+', text: 'AI培训学员满意度' }
    ],
    color: 'accent'
  },
  {
    id: 'health',
    title: '心脑血管健康',
    subtitle: '数字化管理实践者',
    tagline: '产品化思维，数据为核心',
    description: '数据监测 → 问题分析 → 方案定制 → 效果追踪，数字化管理体系',
    highlights: [
      { icon: '🏥', label: '多年', text: '大健康慢病赛道深耕' },
      { icon: '📊', label: '专业', text: '数据资产管理师资质' },
      { icon: '🧠', label: '17年', text: '产品思维沉淀' },
      { icon: '🔗', label: '融合', text: 'AI+健康实践经验' }
    ],
    color: 'secondary'
  }
];

// 恋恋的核心能力
const lianlianCoreCapabilities: CoreCapability[] = [
  {
    id: 'anti-aging',
    title: '女性全生命周期抗衰',
    subtitle: '卵巢健康管理专家',
    tagline: '以肠为根，以轴为核',
    description: '从肠道→激素→神经→循环→免疫全身轴联动，重建卵巢健康内环境',
    highlights: [
      { icon: '❤️', label: 'AMH', text: '卵巢功能精准管理' },
      { icon: '🦠', label: '肠道', text: '微生态修复专家' },
      { icon: '📊', label: '动态', text: '可视化检测分析' },
      { icon: '🤖', label: 'AI', text: '智能健康分析' }
    ],
    color: 'accent'
  },
  {
    id: 'family-health',
    title: '家庭健康规划',
    subtitle: '院前长寿管理体系',
    tagline: '一人学习，全家健康',
    description: '从个人健康到家庭长寿，长期、科学、可传承的健康顶层设计',
    highlights: [
      { icon: '👨‍👩‍👧‍👦', label: '100+', text: '深度服务家庭' },
      { icon: '🍳', label: '24年', text: '健康美食规划' },
      { icon: '💪', label: '6年', text: '健康管理实战' },
      { icon: '🚀', label: '11年', text: '连续创业经验' }
    ],
    color: 'secondary'
  }
];

// 默认成就数据
const defaultAchievements: Achievement[] = [
  { value: '2500万', label: '数字化转型项目', icon: '📈' },
  { value: '672万', label: '智能系统落地项目', icon: '🤖' },
  { value: '30+', label: '团队管理规模', icon: '👥' },
  { value: 'TOP1', label: '数据要素大赛金融赛道', icon: '🏆' }
];

// 恋恋的成就数据
const lianlianAchievements: Achievement[] = [
  { value: '100+', label: '深度服务家庭', icon: '👨‍👩‍👧‍👦' },
  { value: '24年', label: '西餐/甜品主厨', icon: '👨‍🍳' },
  { value: '6年', label: '健康管理实战', icon: '💪' },
  { value: '11年', label: '连续创业', icon: '🚀' }
];

// 获取核心能力：优先 API features，然后根据租户选择默认
const coreCapabilities: CoreCapability[] =
  (tenantConfig?.features?.coreCapabilities as CoreCapability[])?.length > 0
    ? tenantConfig.features.coreCapabilities
    : CURRENT_SUBDOMAIN === 'lianlian'
      ? lianlianCoreCapabilities
      : defaultCoreCapabilities;

// 获取成就数据：优先 API features，然后根据租户选择默认
const achievements: Achievement[] =
  (tenantConfig?.features?.achievements as Achievement[])?.length > 0
    ? tenantConfig.features.achievements
    : CURRENT_SUBDOMAIN === 'lianlian'
      ? lianlianAchievements
      : defaultAchievements;

// 资质与头衔
const credentials = apiHonors.length > 0 ? apiHonors.map((h: any) => ({
  icon: honorTypeEmoji[h.type] || '🏆',
  title: h.title,
  desc: h.description || h.issuer || ''
})) : [
  { icon: '🏗️', title: '系统架构师', desc: '系统架构设计师资质' },
  { icon: '💰', title: '基金从业资格证', desc: '基金行业专业资质' },
  { icon: '📊', title: '数据资产管理师', desc: '全国首批认证资质' },
  { icon: '🚀', title: '楼友会创业导师', desc: '创业指导与孵化服务' },
  { icon: '🏛️', title: '数字文旅专家顾问', desc: '重庆市文旅研究会数字文旅专委会' },
  { icon: '🏝️', title: '海南岛AI健康大使', desc: 'AI+健康推广大使' }
];

// 站点信息
const siteName = tenantConfig?.name || '邱小亮';
const sectionSubtitle = tenantConfig?.seoConfig?.description?.substring(0, 30) ||
  (CURRENT_SUBDOMAIN === 'lianlian'
    ? '以肠为根 · 以轴为核 · 守护卵巢 · 规划一生健康'
    : '让AI落地企业，让健康有数据支撑');
---

<section id="about" class="section relative overflow-hidden">
  <!-- 背景装饰 -->
  <div class="absolute inset-0 bg-gradient-to-b from-background to-surface"></div>

  <div class="container relative z-10">
    <!-- 标题 -->
    <div class="text-center mb-16">
      <span class="data-badge mb-4 inline-flex">关于我</span>
      <h2 class="section-title mb-3">核心能力</h2>
      <p class="section-subtitle">{sectionSubtitle}</p>
    </div>

    <!-- 核心能力卡片 -->
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 lg:gap-8 mb-12">
      {coreCapabilities.map((capability) => (
        <div class="card group relative overflow-hidden">
          <!-- 顶部渐变条 -->
          <div class={`absolute top-0 left-0 right-0 h-1 ${
            capability.color === 'accent' ? 'bg-gradient-to-r from-accent to-purple-500' : 'bg-gradient-to-r from-secondary to-emerald-400'
          }`}></div>

          <!-- 能力头部 -->
          <div class="flex items-start gap-4 mb-6">
            <div class={`icon-box ${capability.color === 'accent' ? '' : 'icon-box-secondary'}`}>
              <svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
              </svg>
            </div>
            <div class="flex-1">
              <div class="flex items-center gap-2 mb-1">
                <h3 class="text-xl font-bold text-text-primary">{capability.title}</h3>
              </div>
              <p class={`text-sm font-medium ${capability.color === 'accent' ? 'text-accent' : 'text-secondary'}`}>{capability.subtitle}</p>
            </div>
          </div>

          <!-- 标语 -->
          <div class="mb-6">
            <p class="text-sm text-text-secondary italic mb-2">{capability.tagline}</p>
            <p class="text-sm text-text-primary font-mono">{capability.description}</p>
          </div>

          <!-- 履历亮点 -->
          <div class="grid grid-cols-2 gap-3">
            {capability.highlights.map((highlight) => (
              <div class="flex items-center gap-2 p-2 rounded-lg bg-background/50 dark:bg-slate-800/50">
                <span class="text-lg">{highlight.icon}</span>
                <div>
                  <span class={`text-sm font-bold ${capability.color === 'accent' ? 'text-accent' : 'text-secondary'}`}>{highlight.label}</span>
                  <span class="text-xs text-text-secondary block">{highlight.text}</span>
                </div>
              </div>
            ))}
          </div>
        </div>
      ))}
    </div>

    <!-- 成就数据 -->
    <div class="card mb-8">
      <h3 class="text-center text-sm font-semibold text-text-primary mb-6 flex items-center justify-center gap-2">
        <svg class="w-5 h-5 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4M7.835 4.697a3.42 3.42 0 001.946-.806 3.42 3.42 0 014.438 0 3.42 3.42 0 001.946.806 3.42 3.42 0 013.138 3.138 3.42 3.42 0 00.806 1.946 3.42 3.42 0 010 4.438 3.42 3.42 0 00-.806 1.946 3.42 3.42 0 01-3.138 3.138 3.42 3.42 0 00-1.946.806 3.42 3.42 0 01-4.438 0 3.42 3.42 0 00-1.946-.806 3.42 3.42 0 01-3.138-3.138 3.42 3.42 0 00-.806-1.946 3.42 3.42 0 010-4.438 3.42 3.42 0 00.806-1.946 3.42 3.42 0 013.138-3.138z" />
        </svg>
        实战成就
      </h3>
      <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
        {achievements.map((achievement) => (
          <div class="text-center p-4 rounded-xl bg-gradient-to-br from-accent/5 to-purple-500/5 dark:from-accent/10 dark:to-purple-500/10">
            <span class="text-2xl mb-2 block">{achievement.icon}</span>
            <div class="stat-number text-2xl">{achievement.value}</div>
            <div class="stat-label">{achievement.label}</div>
          </div>
        ))}
      </div>
    </div>

    <!-- 资质与头衔 -->
    <div class="card">
      <h3 class="text-center text-sm font-semibold text-text-primary mb-6 flex items-center justify-center gap-2">
        <svg class="w-5 h-5 text-accent" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4M7.835 4.697a3.42 3.42 0 001.946-.806 3.42 3.42 0 014.438 0 3.42 3.42 0 001.946.806 3.42 3.42 0 013.138 3.138 3.42 3.42 0 00.806 1.946 3.42 3.42 0 010 4.438 3.42 3.42 0 00-.806 1.946 3.42 3.42 0 01-3.138 3.138 3.42 3.42 0 00-1.946.806 3.42 3.42 0 01-4.438 0 3.42 3.42 0 00-1.946-.806 3.42 3.42 0 01-3.138-3.138 3.42 3.42 0 00-.806-1.946 3.42 3.42 0 010-4.438 3.42 3.42 0 00.806-1.946 3.42 3.42 0 013.138-3.138z" />
        </svg>
        资质与头衔
      </h3>
      <div class="grid grid-cols-2 md:grid-cols-3 gap-4">
        {credentials.map((credential) => (
          <div class="flex items-center gap-3 p-3 rounded-xl bg-gradient-to-br from-accent/5 to-purple-500/5 dark:from-accent/10 dark:to-purple-500/10 hover:from-accent/10 hover:to-purple-500/10 transition-colors">
            <span class="text-2xl">{credential.icon}</span>
            <div>
              <p class="text-sm font-semibold text-text-primary">{credential.title}</p>
              <p class="text-xs text-text-secondary">{credential.desc}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>
